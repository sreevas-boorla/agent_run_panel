# Agent Run Panel (Demo)

This is a minimal demo of the Agent Run Panel using React + Tailwind.

Quick start:

```bash
cd /workspace/agent-run-panel
npm install
npm run dev
```

Open the dev server shown by Vite (default http://localhost:5173).
